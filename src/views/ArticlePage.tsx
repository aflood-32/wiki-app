import React from "react";
import ArticleContainer from "../components/ArticleContainer.component";

const ArticlePage: React.FC = () => (
  <div className="article-page">
    <ArticleContainer />
  </div>
);

export default ArticlePage;
